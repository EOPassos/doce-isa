# Anotações de 01/06/2026


1.  `git status`
2.  `git add .`
3.  `git status`
4.  `git commit -m "Alteração do readme"`
5.  `git status`
6.  `git push`
7.  `git status`
8.  `sudo apt update`
9.  `history`
10.  `git pull`
11.  `uv`
12.  `curl -LsSf https://astral.sh/uv/install.sh | sh`
13.  `uv`
14.  `zenscial`
15.  `zensical`
16.  `uv tool`
17.  `uv tool list`
18.  `uv tool install mkdocs zensical`
19.  `uv tool install mkdocs`
20.  `uv tool install zensical`
21.  `zensical`
22.  `zensical new`
23.  `git status`
24.  `git add .`
25.  `git status`
26.  `git commit -m "Configuração inicial do Zensical"`
27.  `git status`
28.  `git push`
29.  `git fetch`
30.  `git merge`
31.  `git pull`
32.  `git push`
33.  `git pull`
34.  `cd .github/workflows/`
35.  `ls`
36.  `git rm static.yml; git mv docs.yml static.yml `
37.  `git status`
38.  `git commit -m 'Site habilitado'`
39.  `git push`
40.  `mkdir demo`
41.  `ls`
42.  `cd demo`
43.  `ls`
44.  `touch pedra.md papel.md tesoura.md`
45.  `ls`
46.  `nano pedra.md`
47.  `nano papel.md`
48.  `nano tesoura.md`
49.  `ls`
50.  `cd docs`
51.  `ls`
52.  `mkdir diario`
53.  `ls`
54.  `cd diario`
55.  `touch anotacoes-2026-06-01.md`
56.  `ls`
57.  `nano index.md`
58.  `nano anotacoes-2026-06-01.md `
59.  `touch anotacoes-2026-05-20.md`
60.  `nano anotacoes-2026-05-20.md `
61.  `history `
62.  `history >> anotacoes-2026-06-01.md `
